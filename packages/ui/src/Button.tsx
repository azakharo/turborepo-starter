'use client';

import * as React from 'react';

//let myfunc = (a, b) => a + b;

export const Button = () => {
  // let myvar = 1;
  console.log('ha-ha-ha');

  return <button onClick={() => alert('boop')}>Boop</button>;
};
