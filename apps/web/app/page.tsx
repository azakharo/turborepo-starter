import {Button, Header} from 'ui';

//let myfunc2 = (a, b) => a + b;

export default function Page() {
  const myvar3 = 1;
  console.log('ha-ha-ha', myvar3);

  return (
    <>
      <Header text="Web" />
      <Button />
    </>
  );
}
